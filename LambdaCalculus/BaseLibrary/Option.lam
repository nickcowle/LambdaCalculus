none := λ n λ s . n

some := λ v λ n λ s . s v

bind := λ o λ f . o Option.none f

return := Option.some

apply := Combinators.bindToApply Option.bind Option.return

map := Combinators.applyToMap Option.apply Option.return

isNone := λ o . o Bool.true (λ v . Bool.false)

isSome := λ o . o Bool.false (λ v . Bool.true)